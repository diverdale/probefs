---
phase: 06-preview-pane-and-status-bar
plan: "05"
type: execute
wave: 4
depends_on:
  - "06-04"
files_modified: []
autonomous: false
requirements:
  - PREV-01
  - PREV-02
  - DISP-04

must_haves:
  truths:
    - "Focusing a text file displays a syntax-highlighted preview in the right pane without blocking navigation"
    - "Focusing a directory displays that directory's file listing in the right pane"
    - "The status bar always shows the current path, total item count, and available free space"
    - "The footer shows key hint labels for primary navigation keys"
  artifacts:
    - path: "src/probefs/widgets/preview_pane.py"
      provides: "Full two-mode preview implementation"
      contains: "ContentSwitcher"
    - path: "src/probefs/widgets/status_bar.py"
      provides: "StatusBar widget"
      contains: "class StatusBar"
    - path: "src/probefs/screens/main.py"
      provides: "Wired layout with StatusBar + Footer"
      contains: "StatusBar"
    - path: "src/probefs/probefs.tcss"
      provides: "Correct three-pane layout with status bar"
      contains: "#panes"
  key_links:
    - from: "user"
      to: "preview pane"
      via: "cursor movement in current pane triggers preview update"
      pattern: "visual observation"
    - from: "user"
      to: "status bar"
      via: "navigation updates path/count/space in status bar"
      pattern: "visual observation"
---

<objective>
Human verification of all Phase 6 features: syntax-highlighted file preview, directory preview, status bar, and Footer key hints.

Purpose: Visual confirmation that the three-pane layout, preview pane, status bar, and footer all work correctly together. This is the final gate before Phase 6 is marked complete.
Output: Human-verified Phase 6 feature set. Phase 6 complete.
</objective>

<execution_context>
@/Users/dalwrigh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dalwrigh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Verify Phase 6 — preview pane, status bar, and footer</name>
  <action>Run `uv run probefs` from the project root and execute the 7 verification steps below.</action>
  <what-built>
Phase 6 complete implementation:
- PreviewPane: syntax-highlighted file preview (Rich Syntax, line numbers, ansi_dark theme) for text files; directory listing for directories; "Binary file" message for binaries
- StatusBar: one-line bar showing current path, item count, and free space (GB)
- Footer: key hint bar showing j/k/l/h/./q labels below the status bar
- Layout refactor: #panes Horizontal container with 1fr height; StatusBar and Footer docked below
  </what-built>
  <how-to-verify>
Launch the app from the project root:
```
cd /Users/dalwrigh/dev/probefs && uv run probefs
```

Work through each verification step:

**1. Layout integrity**
- Three panes fill the top portion of the screen
- A single-line status bar appears near the bottom (above the footer)
- A key-hint Footer appears at the very bottom (should show: j Down, k Up, l Open, h Back, . Hidden, q Quit or similar)

**2. Status bar (DISP-04)**
- The status bar shows the current path (your home directory on launch)
- The status bar shows an item count (e.g. "42 items")
- The status bar shows free space (e.g. "187.3 GB free")
- Navigate to a subdirectory (press l on a directory) — status bar path, count, and free space all update

**3. File preview — text file (PREV-01)**
- Navigate to a directory containing source code files (e.g. ~/dev/probefs/src/probefs)
- Move the cursor over a .py file — the right pane shows syntax-highlighted Python code with line numbers
- Move the cursor over a .tcss file — the right pane updates to show the CSS content
- Navigation remains responsive while the preview loads (no UI freeze)

**4. Directory preview (PREV-02)**
- Move the cursor over a directory — the right pane shows that directory's file listing (same style as the left panes)
- Navigate back (press h) — parent and current panes update; preview pane updates for the new focused entry

**5. Binary file handling**
- Navigate to a directory with image files or executables (e.g. /usr/bin or ~/Downloads)
- Move the cursor over a binary file — the right pane shows a dim "Binary file — preview unavailable" message (not an error crash)

**6. Fast cursor movement**
- Hold j/k to scroll quickly through a directory with many files
- Preview updates correctly to the final position (no stale preview from an earlier file)

**7. Existing features regression check**
- Press . to toggle hidden files — still works
- Press y on a file to start a copy operation — InputDialog still appears
- Press q to quit — app exits cleanly
  </how-to-verify>
  <verify>All 7 steps above pass without errors or crashes.</verify>
  <done>Human confirmed all Phase 6 features work: preview pane (file + directory modes), status bar (path/count/space), footer (key hints), and no Phase 5 regressions.</done>
  <resume-signal>Type "approved" if all 7 verification steps pass, or describe any issues found.</resume-signal>
</task>

</tasks>

<verification>
Human verified all Phase 6 success criteria:
1. Layout: three panes + status bar + footer renders correctly
2. DISP-04: status bar shows path, item count, free space — updates on navigation
3. PREV-01: text files show syntax-highlighted preview with line numbers
4. PREV-02: directories show file listing in preview pane
5. Binary file: shows informative message, no crash
6. Fast scrolling: no stale previews
7. All Phase 5 file operations still work
</verification>

<success_criteria>
Human confirmation ("approved") that:
- Syntax-highlighted file preview renders without blocking navigation (PREV-01)
- Directory listing renders in preview pane when a directory is focused (PREV-02)
- Status bar shows path, item count, and free space, updating on navigation (DISP-04)
- Footer key hints visible for primary navigation keys
- No regressions in navigation or file operations from Phase 5
</success_criteria>

<output>
After completion, create `.planning/phases/06-preview-pane-and-status-bar/06-05-SUMMARY.md`

Also update STATE.md: Phase 6 complete, progress 100%, current focus: Phase 7.
</output>
