---
phase: 04-keybinding-system-and-config-infrastructure
plan: "02"
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified: []
autonomous: false
requirements:
  - KEYS-01

must_haves:
  truths:
    - "User edits ~/.probefs/probefs.yaml keybindings section and the remapped key works on next launch"
    - "The original default key no longer works after remapping (replace semantics confirmed)"
    - "App launches cleanly with a keybindings section present — no crash, no warning"
  artifacts:
    - path: "src/probefs/app.py"
      provides: "_setup_keybindings() reads YAML config and applies set_keymap()"
      contains: "set_keymap"
  key_links:
    - from: "~/.probefs/probefs.yaml"
      to: "src/probefs/app.py"
      via: "load_config() -> config['keybindings'] -> set_keymap()"
      pattern: "keybindings:"
---

<objective>
Human verification that keybinding overrides work end-to-end: user edits config, relaunches, and confirms the remapped key is live.

Purpose: The only way to verify this is interactive — a human must operate the running TUI and confirm key behavior changed. Code inspection alone cannot prove the runtime behavior.

Output: Confirmed working keybinding override system.
</objective>

<execution_context>
@/Users/dalwrigh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dalwrigh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/04-keybinding-system-and-config-infrastructure/04-01-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Verify keybinding overrides work end-to-end</name>
  <what-built>
    ProbeFSApp.BINDINGS with stable probefs.* IDs on all 11 bindings, _setup_keybindings() method that reads keybindings: from ~/.probefs/probefs.yaml and calls set_keymap(), and docs/keybindings.md reference.
  </what-built>
  <how-to-verify>
    **Step 1 — Confirm q quits with no config changes.**

    Launch probefs:
    ```
    cd /Users/dalwrigh/dev/probefs
    uv run probefs
    ```
    Press `q`. The app should exit cleanly.
    Press `ctrl+c` to exit if needed. Relaunch and confirm `q` exits.

    **Step 2 — Remap cursor_down to n, relaunch, confirm.**

    Add this to ~/.probefs/probefs.yaml (create the file if it doesn't exist):
    ```yaml
    keybindings:
      probefs.cursor_down: "n"
    ```

    Launch probefs: `uv run probefs`

    Verify:
    - Press `n` — cursor moves DOWN. (Expected: YES)
    - Press `j` — cursor does NOT move. (Expected: j no longer works — replace semantics)
    - Press `k` — cursor moves UP. (Expected: YES — unaffected binding)
    - Press `down` arrow — cursor moves DOWN. (Expected: YES — probefs.cursor_down_arrow unaffected)
    - Press `q` to quit.

    **Step 3 — Add a key while keeping the original.**

    Update ~/.probefs/probefs.yaml:
    ```yaml
    keybindings:
      probefs.cursor_down: "n,j"
    ```

    Launch probefs: `uv run probefs`

    Verify:
    - Press `n` — cursor moves DOWN. (Expected: YES)
    - Press `j` — cursor moves DOWN. (Expected: YES — kept explicitly)
    - Press `q` to quit.

    **Step 4 — Remove the keybindings section (clean up).**

    Remove the keybindings section from ~/.probefs/probefs.yaml (or delete the file).
    Launch probefs: `uv run probefs`

    Verify all defaults work: j/k navigation, h/l dir traversal, . toggle, q quit.

    Quit with `q`.
  </how-to-verify>
  <resume-signal>Type "approved" if all 4 steps pass, or describe what failed.</resume-signal>
</task>

</tasks>

<verification>
Human confirms:
1. q exits the app
2. Remapping probefs.cursor_down to "n" makes n work and j stop working
3. Using "n,j" keeps j working while adding n
4. Removing the keybindings section restores all defaults
</verification>

<success_criteria>
- Keybinding override takes effect on next launch after editing probefs.yaml
- Replace semantics confirmed: remapped key replaces, does not extend
- Additive mapping ("n,j") keeps original key working
- App launches cleanly with keybindings config present
- All defaults restored after removing keybindings section
</success_criteria>

<output>
After completion, create `.planning/phases/04-keybinding-system-and-config-infrastructure/04-02-SUMMARY.md`
</output>
